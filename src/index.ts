/**
 * bibliotecaDataApi description
 *
 * @todo scope-1 Something to be done
 * @todo scope-2 Something else to be done
 *
 * @function bibliotecaDataApi
 * @param {string} param
 * @returns {string} result
 */
export const bibliotecaDataApi = (param: string): string => `output "${param}"`
